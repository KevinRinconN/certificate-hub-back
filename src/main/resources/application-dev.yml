spring:
  datasource:
    url: "${MYSQL_URL}"
    username: "${MYSQL_USER}"
    password: "${MYSQL_PASSWORD}"
  jpa:
    hibernate:
      ddl-auto: update
  config:
    activate:
      on-profile: dev
server:
  port: 8080


